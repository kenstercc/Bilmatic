import type { RenderContext } from './node';
import { Node } from './node';
export declare class Image extends Node {
    private sourceImage?;
    constructor(sourceImage?: HTMLImageElement | HTMLCanvasElement | ImageBitmap | undefined);
    updateBitmap(newBitmap: HTMLImageElement | ImageBitmap | HTMLCanvasElement, bitmapPixelRatio: number, x: number, y: number): void;
    x: number;
    y: number;
    width: number;
    height: number;
    opacity: number;
    render(renderCtx: RenderContext): void;
}
